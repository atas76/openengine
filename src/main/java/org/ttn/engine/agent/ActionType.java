package org.ttn.engine.agent;

public enum ActionType {
    Implicit,
    Long, BounceOff, Shoot, Move, Cross, Dribble,
    Pass, BackPass, ParallelPass, DiagonalPass, ForwardPass, WidePass, HighPass,
    Clear,
    Default
}
